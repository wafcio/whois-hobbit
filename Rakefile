require_relative 'config/application'

Dir.glob(File.join(Whois.root, 'lib', 'tasks', '*.rake')).each { |file| import file }
Dir.glob(File.join(Whois.root, 'lib', 'generators', '**', '*.rake')).each { |file| import file }
